{
  "name": "informegaming",
  "version": "1.0.0",
  "description": "Sitio de noticias y juegos gratis - Real Free Games Data Pipeline",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-noticias.cjs && vite build && node scripts/generate-sitemap.cjs",
    "build:noticias": "node scripts/build-noticias.cjs",
    "build:sitemap": "node scripts/generate-sitemap.cjs",
    "preview": "vite preview",
    "test": "npm run test:worker && npm run test:e2e",
    "test:worker": "cd worker && npm test",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "playwright": "^1.47.0",
    "tsx": "^4.19.0",
    "vite": "^5.4.0",
    "vite-plugin-sitemap": "^0.6.0"
  },
  "dependencies": {
    "astro": "^7.3.1"
  }
}
